- form_for @user , :url => subdomain_users_url(@account) do |form|
  = form.label :email
  = form.text_field :email

  = form.label :first_name, "First Name"
  = form.text_field :first_name

  = form.label :last_name, "Last Name"
  = form.text_field :last_name

  = form.submit "add"
